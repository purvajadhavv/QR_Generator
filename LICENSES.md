# Licenses

The QR encoder in `src/qr-engine.js` is a compact browser implementation derived from permissively licensed QR construction techniques. The project itself is provided without a backend or external service dependency.
